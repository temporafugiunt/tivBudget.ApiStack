apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tiv-api-budget.fullname" . }}-test-connection"
  labels:
    {{- include "tiv-api-budget.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tiv-api-budget.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
